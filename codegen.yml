schema: "src/graphql/*.gql"
generates:
  "src/@types/resolvers-types.ts":
    plugins:
      - typescript
      - typescript-resolvers
    config:
      mapperTypeSuffix: Model
      mappers:
        Entry: '@prisma/client/index.d#Entry'
        Tag: '@prisma/client/index.d#Tag'
